import type { Metadata } from "next";

export const metadata: Metadata = { title: "HR Letter Generator · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

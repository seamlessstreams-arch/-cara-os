import type { Metadata } from "next";

export const metadata: Metadata = { title: "After-School Clubs · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Child Voice Presence · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

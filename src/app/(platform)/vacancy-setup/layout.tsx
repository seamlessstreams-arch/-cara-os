import type { Metadata } from "next";

export const metadata: Metadata = { title: "Vacancy Setup · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

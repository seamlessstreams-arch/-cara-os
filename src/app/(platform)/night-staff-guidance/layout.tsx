import type { Metadata } from "next";

export const metadata: Metadata = { title: "Night Staff Guide · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

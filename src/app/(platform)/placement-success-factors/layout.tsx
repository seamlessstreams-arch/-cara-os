import type { Metadata } from "next";

export const metadata: Metadata = { title: "Success Factors · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

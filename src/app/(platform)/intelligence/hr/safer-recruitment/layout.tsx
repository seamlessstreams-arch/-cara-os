import type { Metadata } from "next";

export const metadata: Metadata = { title: "Safer Recruitment Gate · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Ask CARA Governance · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

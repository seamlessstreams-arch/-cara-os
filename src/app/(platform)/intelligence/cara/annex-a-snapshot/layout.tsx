import type { Metadata } from "next";

export const metadata: Metadata = { title: "Annex A Snapshot · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

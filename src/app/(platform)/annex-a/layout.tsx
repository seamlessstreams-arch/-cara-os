import type { Metadata } from "next";

export const metadata: Metadata = { title: "Annex A Readiness · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

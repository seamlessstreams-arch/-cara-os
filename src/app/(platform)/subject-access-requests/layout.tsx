import type { Metadata } from "next";

export const metadata: Metadata = { title: "Subject Access Requests · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

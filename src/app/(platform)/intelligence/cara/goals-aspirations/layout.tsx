import type { Metadata } from "next";

export const metadata: Metadata = { title: "Goals & Aspirations · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

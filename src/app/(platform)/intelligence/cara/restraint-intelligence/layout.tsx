import type { Metadata } from "next";

export const metadata: Metadata = { title: "Physical Intervention · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

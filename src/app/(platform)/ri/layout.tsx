import type { Metadata } from "next";

export const metadata: Metadata = { title: "RI Command Centre · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

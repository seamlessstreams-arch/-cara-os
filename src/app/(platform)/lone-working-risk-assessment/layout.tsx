import type { Metadata } from "next";

export const metadata: Metadata = { title: "Lone Working RA · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

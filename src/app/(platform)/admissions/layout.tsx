import type { Metadata } from "next";

export const metadata: Metadata = { title: "Admissions · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

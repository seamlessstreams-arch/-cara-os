import type { Metadata } from "next";

export const metadata: Metadata = { title: "Incident Trends · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

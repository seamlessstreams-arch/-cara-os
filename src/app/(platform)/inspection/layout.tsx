import type { Metadata } from "next";

export const metadata: Metadata = { title: "Inspection · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

// /cara/governance/routing renders light without PageShell (its sibling
// governance pages bring their own dark shell) → apply the dark skin.
export { default } from "@/components/layout/dark-skin-layout";

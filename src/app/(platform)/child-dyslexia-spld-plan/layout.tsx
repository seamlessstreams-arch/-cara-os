import type { Metadata } from "next";

export const metadata: Metadata = { title: "Dyslexia / SpLD · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Reg 45 Evidence Bank · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

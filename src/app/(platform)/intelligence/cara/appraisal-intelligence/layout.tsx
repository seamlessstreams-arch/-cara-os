import type { Metadata } from "next";

export const metadata: Metadata = { title: "Appraisal Intelligence · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "House Rules · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

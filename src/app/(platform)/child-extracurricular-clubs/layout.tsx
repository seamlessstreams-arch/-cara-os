import type { Metadata } from "next";

export const metadata: Metadata = { title: "Clubs & Societies · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

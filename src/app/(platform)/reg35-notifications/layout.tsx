import type { Metadata } from "next";

export const metadata: Metadata = { title: "Reg 35 Notifications · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Relational Safety Map · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

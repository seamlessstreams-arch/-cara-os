import type { Metadata } from "next";

export const metadata: Metadata = { title: "Workforce Command · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

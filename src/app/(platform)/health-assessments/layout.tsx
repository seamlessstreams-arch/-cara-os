import type { Metadata } from "next";

export const metadata: Metadata = { title: "Health Assessments · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

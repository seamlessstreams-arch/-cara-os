import type { Metadata } from "next";

export const metadata: Metadata = { title: "Med Near Misses · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

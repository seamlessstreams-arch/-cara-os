import type { Metadata } from "next";

export const metadata: Metadata = { title: "Strategic Risks · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

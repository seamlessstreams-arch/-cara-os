import type { Metadata } from "next";

export const metadata: Metadata = { title: "Recruitment Command · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

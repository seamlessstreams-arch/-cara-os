import type { Metadata } from "next";

export const metadata: Metadata = { title: "Serious Incident Reviews · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

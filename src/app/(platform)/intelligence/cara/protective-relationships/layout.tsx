import type { Metadata } from "next";

export const metadata: Metadata = { title: "Protective Relationships · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

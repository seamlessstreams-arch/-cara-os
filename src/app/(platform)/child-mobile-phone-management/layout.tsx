import type { Metadata } from "next";

export const metadata: Metadata = { title: "Mobile Phones · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

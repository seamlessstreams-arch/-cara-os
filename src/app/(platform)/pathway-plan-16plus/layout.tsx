import type { Metadata } from "next";

export const metadata: Metadata = { title: "Pathway Plan (16+) · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

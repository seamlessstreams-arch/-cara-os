import type { Metadata } from "next";

export const metadata: Metadata = { title: "Relationship Index · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

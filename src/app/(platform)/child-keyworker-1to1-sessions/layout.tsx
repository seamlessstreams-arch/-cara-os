import type { Metadata } from "next";

export const metadata: Metadata = { title: "1:1 Sessions · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Medication Audit · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

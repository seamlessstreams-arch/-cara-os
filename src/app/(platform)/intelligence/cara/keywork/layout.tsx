import type { Metadata } from "next";

export const metadata: Metadata = { title: "Key Work Builder · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

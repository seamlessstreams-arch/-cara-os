import type { Metadata } from "next";

export const metadata: Metadata = { title: "Statutory Checks · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Feedback on Staff · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

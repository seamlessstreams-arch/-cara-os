import type { Metadata } from "next";

export const metadata: Metadata = { title: "RSE Tracker · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

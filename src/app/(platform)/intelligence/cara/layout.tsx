// The /intelligence/cara/* subtree is mixed: most pages use PageShell (already
// dark), but ~30 Cara-intelligence dashboards render directly and stayed light.
// A segment layout darkens those; it nests harmlessly under the PageShell pages.
export { default } from "@/components/layout/dark-skin-layout";

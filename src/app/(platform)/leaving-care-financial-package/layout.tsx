import type { Metadata } from "next";

export const metadata: Metadata = { title: "Leaving Care Funds · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

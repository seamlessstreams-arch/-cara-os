import type { Metadata } from "next";

export const metadata: Metadata = { title: "Deployment Suitability (Reg 32) · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Reg 44 Visits · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Multi-Agency Meetings · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Job Tracker (16+) · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

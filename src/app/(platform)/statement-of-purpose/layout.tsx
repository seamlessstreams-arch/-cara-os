import type { Metadata } from "next";

export const metadata: Metadata = { title: "Statement of Purpose · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

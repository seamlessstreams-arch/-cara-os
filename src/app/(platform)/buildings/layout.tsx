import type { Metadata } from "next";

export const metadata: Metadata = { title: "Buildings & H&S · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Holiday Planning · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

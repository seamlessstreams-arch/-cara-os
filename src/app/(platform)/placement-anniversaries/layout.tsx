import type { Metadata } from "next";

export const metadata: Metadata = { title: "Anniversaries · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

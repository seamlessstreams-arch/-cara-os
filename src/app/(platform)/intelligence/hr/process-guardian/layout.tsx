import type { Metadata } from "next";

export const metadata: Metadata = { title: "HR Process Guardian · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

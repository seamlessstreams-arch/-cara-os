import type { Metadata } from "next";

export const metadata: Metadata = { title: "LAC Review Prep · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

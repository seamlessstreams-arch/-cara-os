import type { Metadata } from "next";

export const metadata: Metadata = { title: "Deaf & Hearing · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

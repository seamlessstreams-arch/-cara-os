import type { Metadata } from "next";

export const metadata: Metadata = { title: "Positive Achievements · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

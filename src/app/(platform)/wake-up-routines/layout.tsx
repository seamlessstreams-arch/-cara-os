import type { Metadata } from "next";

export const metadata: Metadata = { title: "Wake-Up Routines · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

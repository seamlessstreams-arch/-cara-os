import type { Metadata } from "next";

export const metadata: Metadata = { title: "Practice Action Centre · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

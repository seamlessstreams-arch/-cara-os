import type { Metadata } from "next";

export const metadata: Metadata = { title: "L.I.V.E.R.S. Planner · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

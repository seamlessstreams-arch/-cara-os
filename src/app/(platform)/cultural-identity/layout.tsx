import type { Metadata } from "next";

export const metadata: Metadata = { title: "Cultural Identity · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

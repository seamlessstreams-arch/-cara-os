import type { Metadata } from "next";

export const metadata: Metadata = { title: "Sensory Room Use · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

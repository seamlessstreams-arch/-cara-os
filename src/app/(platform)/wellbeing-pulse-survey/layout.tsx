import type { Metadata } from "next";

export const metadata: Metadata = { title: "Wellbeing Pulse · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Origins & Emergency Follow-ups · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

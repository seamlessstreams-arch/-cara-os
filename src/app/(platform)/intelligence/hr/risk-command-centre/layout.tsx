import type { Metadata } from "next";

export const metadata: Metadata = { title: "HR Risk Command Centre · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

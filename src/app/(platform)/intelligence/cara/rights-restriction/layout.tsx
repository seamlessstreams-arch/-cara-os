import type { Metadata } from "next";

export const metadata: Metadata = { title: "Rights & Restriction · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

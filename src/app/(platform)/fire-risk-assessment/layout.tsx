import type { Metadata } from "next";

export const metadata: Metadata = { title: "Fire Risk Assessment · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

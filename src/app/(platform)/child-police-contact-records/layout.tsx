import type { Metadata } from "next";

export const metadata: Metadata = { title: "Police Contact · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

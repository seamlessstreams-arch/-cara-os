import type { Metadata } from "next";

export const metadata: Metadata = { title: "Significant Events · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

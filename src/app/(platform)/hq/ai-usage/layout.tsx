import type { Metadata } from "next";

export const metadata: Metadata = { title: "AI Usage & Cost · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Med Error Investigations · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

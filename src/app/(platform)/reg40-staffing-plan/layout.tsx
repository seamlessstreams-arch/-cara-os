import type { Metadata } from "next";

export const metadata: Metadata = { title: "Staffing Plan (Reg 40) · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

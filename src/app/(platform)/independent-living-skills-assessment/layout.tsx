import type { Metadata } from "next";

export const metadata: Metadata = { title: "Living Skills Assessment · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

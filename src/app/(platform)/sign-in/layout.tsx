import type { Metadata } from "next";

export const metadata: Metadata = { title: "Shift Sign-In · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

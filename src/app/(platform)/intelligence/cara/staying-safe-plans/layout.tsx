import type { Metadata } from "next";

export const metadata: Metadata = { title: "Staying Safe Plans · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

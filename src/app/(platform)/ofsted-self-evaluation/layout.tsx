import type { Metadata } from "next";

export const metadata: Metadata = { title: "Ofsted Self-Eval · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Bedroom Profile · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

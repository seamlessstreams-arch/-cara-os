import type { Metadata } from "next";

export const metadata: Metadata = { title: "Vehicle Pre-Use · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

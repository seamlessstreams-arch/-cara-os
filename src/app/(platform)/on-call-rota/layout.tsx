import type { Metadata } from "next";

export const metadata: Metadata = { title: "On-Call Rota · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Physio & OT · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

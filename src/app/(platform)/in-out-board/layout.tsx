import type { Metadata } from "next";

export const metadata: Metadata = { title: "In & Out Board · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

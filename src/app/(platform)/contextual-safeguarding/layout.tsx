import type { Metadata } from "next";

export const metadata: Metadata = { title: "Contextual Safeguarding · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

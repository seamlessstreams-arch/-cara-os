import type { Metadata } from "next";

export const metadata: Metadata = { title: "Key Worker Effectiveness · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

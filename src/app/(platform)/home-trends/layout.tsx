import type { Metadata } from "next";

export const metadata: Metadata = { title: "Home Trends · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

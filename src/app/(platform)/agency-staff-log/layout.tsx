import type { Metadata } from "next";

export const metadata: Metadata = { title: "Agency Staff Log · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "De-Escalation Strategy · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Career Pathways · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Menu Planning · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

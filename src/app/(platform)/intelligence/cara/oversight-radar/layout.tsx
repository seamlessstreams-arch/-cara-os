import type { Metadata } from "next";

export const metadata: Metadata = { title: "Oversight Radar · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Reg 45 Evidence · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Self-Harm Plan · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Home Summary Report · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

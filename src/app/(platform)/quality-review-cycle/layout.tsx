import type { Metadata } from "next";

export const metadata: Metadata = { title: "Quality Review (Reg 46) · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

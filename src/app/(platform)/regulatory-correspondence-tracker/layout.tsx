import type { Metadata } from "next";

export const metadata: Metadata = { title: "Regulator Correspondence · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Life Story · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

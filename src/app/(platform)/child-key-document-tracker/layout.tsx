import type { Metadata } from "next";

export const metadata: Metadata = { title: "Key Documents · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

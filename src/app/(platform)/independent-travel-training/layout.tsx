import type { Metadata } from "next";

export const metadata: Metadata = { title: "Travel Training · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

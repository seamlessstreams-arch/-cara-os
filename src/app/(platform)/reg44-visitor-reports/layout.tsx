import type { Metadata } from "next";

export const metadata: Metadata = { title: "Reg 44 Reports · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

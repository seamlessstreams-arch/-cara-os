import type { Metadata } from "next";

export const metadata: Metadata = { title: "Ofsted Evidence Room · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

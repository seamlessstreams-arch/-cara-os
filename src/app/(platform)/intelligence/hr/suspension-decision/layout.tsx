import type { Metadata } from "next";

export const metadata: Metadata = { title: "HR Suspension Tool · Cara" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
